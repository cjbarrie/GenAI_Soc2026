# Python foundations

Work through `python_foundations.ipynb` before Session 1 or use its diagnostic to locate the first unfamiliar unit. The intended time is six to eight hours in short blocks for a complete beginner.

Each cell should be handled in this order: predict the output, run it, explain each line, then change one value. The notebook intentionally uses lists, dictionaries, loops, and small functions before pandas shortcuts.

