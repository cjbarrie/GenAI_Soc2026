# Copy to .env only if your local environment loads it. Never commit .env.
OPENROUTER_API_KEY=
# Course model defaults live in config/course_models.json. Advanced users may
# override them temporarily in the shell when running the preflight script.
